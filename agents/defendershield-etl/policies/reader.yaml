name: etl-reader
description: Read-only access to DefenderShield ETL database

databases:
  - name: primary
    type: postgres
    hostname: ds-etl-postgres
    port: 5432
    database: defendershield
    user: auditor_ro
    password: trust
    env_prefix: PG

tools:
  - "Read"
  - "Bash(psql*)"
  - "Bash(python3*)"
  - "Bash(date*)"
